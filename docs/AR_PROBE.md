# T02 ARローカル試作・G1実機検証

状態: ローカルAR試作を実装済み。b874aabのCIでdevice build、Simulator単体8件・UI3件成功。実機操作は未実施。G1は未判定。

## テスト用の公開境界

`MapGrapherIOSTests/ARPlaneGeometryTests.swift` はアプリモジュールの次の純粋計算を指定する。実装前のREDと実装後7件のGREENをGitHub Actionsで確認済み。

```swift
struct ARPlaneGeometry {
    let widthM: Double
    let heightM: Double
    init(pixelWidth: Double, pixelHeight: Double, displayWidthM: Double) throws
}
```

ピクセル幅・高さは正のfinite値、表示幅は0.1〜10m。高さは画像の縦横比から求める。計算結果が0または非finiteになる入力を拒否する。fixtureは実行時にCoreGraphicsで生成する透明な1000×500px画像で、上向き矢印と左上の丸を持つ。表示幅1mなら高さ0.5m。ARKit/RealityKitと画像デコードの成否はこの単体テストでは判定しない。

配置は検出済み平面へのraycast結果から`ARPlaneAnchor`を要求する。Appleの[ARPlaneAnchor座標系](https://developer.apple.com/documentation/arkit/arplaneanchor)に合わせ、平面の回転とXZ面・Y法線を使い、位置だけraycast交点へ置き換える。透過板は同じXZ面に生成する。水平・垂直とも独自の90度回転は加えない。見た目と追跡の成否はA04/A05で実機確認する。

iOS 17では素材の既定の表面表示を使う。iOS 18以降は`faceCulling = .none`で裏側からも表示する。実機記録ではOSと観察側を記録し、表面が床・壁の観察側へ向いていることを確認する。

## 実行前に記録する項目

| 項目 | 記録 |
|---|---|
| 実施日・担当 | 未実施 |
| 非LiDARでARKit対応のiPhone機種 | 未確認 |
| iOS・Xcode版 | 未確認 |
| 対象commit・build構成 | 未確認 |
| 試験素材 | 個人情報を含まない1000×500pxの透過画像を実行時生成。実機での透過表示は未確認 |
| 検証場所 | 平らな床と壁がある安全な場所。正確な座標は記録しない |
| 証拠 | 個人情報の映らないスクリーンショット/動画と結果ログの保存先を後で記入 |

Macでは`IOS_SIMULATOR_ID`に実在するUDIDを設定して`bash scripts/verify-ios.sh unit`、`bash scripts/verify-ios.sh build`を実行する。実機では開発用の署名設定を使い、同じcommitをインストールする。Simulatorの結果は実機ARの合格に数えない。

## 非LiDAR実機の受入記録

各行を操作し、実測結果・証拠とともにPASS/FAILへ更新する。現時点では全行未実施。

| ID | 操作 | 合格条件 | 状態 | 実測・証拠 |
|---|---|---|---|---|
| A01 | AR試作を開き、カメラ許可を与える | `ARWorldTrackingConfiguration.isSupported` が真の端末でhorizontal/vertical検出が始まる | 未実施 | — |
| A02 | 初回カメラ許可を拒否し、設定で許可して戻る | 拒否理由と設定導線が分かり、再開できる。無限に許可ダイアログを出さない | 未実施 | — |
| A03 | 特徴の少ない面をタップし、raycastに失敗させる | 配置せず「面を探しています」と示す | 未実施 | — |
| A04 | 水平な床を検出して透過PNGを配置する | 画像板が平面に留まり、縁に黒背景が出ない | 未実施 | — |
| A05 | 垂直な壁を検出して同じPNGを配置する | 表裏・向きが確認でき、画像板が壁に留まる | 未実施 | — |
| A06 | 1000×500pxの画像を実幅1mで表示し、横・縦から見る | 高さは0.5m相当、縦横比を保ち、不自然な反転や引き伸ばしがない | 未実施 | — |
| A07 | 配置済みの板を解除し、別の面へ再配置する | 古いanchorが残らず、透過板は1枚だけ表示される | 未実施 | — |
| A08 | 端末を動かして追跡limitedを起こし、安定した面へ戻す | 理由と再試行/戻る操作が示され、追跡回復後に使える | 未実施 | — |
| A09 | 通話相当の割込みを起こしてアプリへ戻る | セッション中断を扱い、必要なら再配置を案内する。クラッシュしない | 未実施 | — |
| A10 | AR中にbackgroundへ移し、前景へ戻る | cameraをpauseし、復帰後に追跡・配置を安全に再開する | 未実施 | — |
| A11 | AR画面を5分連続利用する | 追跡・描画が継続し、クラッシュと明らかな発熱による操作不能がない | 未実施 | — |
| A12 | AR画面へ10回入退場する | 毎回カメラを解放し、二重session・黒画面・クラッシュがない | 未実施 | — |
| A13 | 配置後にsnapshotを作る | 返却画像にARの板と背景が含まれ、カメラの向きと縦横比が正しい | 未実施 | — |
| A14 | snapshot画像を共有シートで写真共有する | 利用者操作で共有でき、AR driverが写真ライブラリへ直接書き込まない | 未実施 | — |
| A15 | AR非対応状態を模擬または対応外端末で確認する | 開始前に理由と戻る導線を示し、カメラ上の2D画像で代用しない | 未実施 | — |
| A16 | 写真共有を連打し、撮影中に停止・戻る・タブ切替・backgroundへ移す | 多重撮影や退出後の古い共有表示がなく、再入場して撮影できる | 未実施 | — |
| A17 | 共有シートから写真保存を選び、許可・拒否を確認する | 日本語の用途説明が示され、拒否してもクラッシュしない | 未実施 | — |

G1判定はA01〜A14・A16・A17の実機結果と、機種・OS・commit・証拠が揃った時に行う。A15は対応外環境の経路として別に記録する。技術的失敗はFAILとし、方式を再設計してから再試験する。サーバー素材・位置ゲート・Androidとの共有anchorはT02の対象外。
